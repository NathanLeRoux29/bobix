# Notes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implémenter le module Notes frontend — éditeur Markdown deux modes (texte brut / aperçu WYSIWYG), onglets, explorateur de fichiers modal liste/grille, auto-save, panel Bob masquable.

**Architecture:** `NotesView` est un shell layout. Chaque partie (ActionBar, Tabs, Editor, Modal, BobPanel) est un composant isolé. `useNotesStore` gère l'état partagé (onglets ouverts, note active, auto-save). Le backend FastAPI est déjà prêt — aucune modification backend dans ce plan.

**Tech Stack:** Vue 3, TypeScript, Pinia, `marked`, `dompurify`, Vitest.

---

## File Map

**Créer :**
- `frontend/src/types/notes.ts` — interfaces TypeScript Note et Folder
- `frontend/src/stores/notes.ts` — useNotesStore (état + actions + auto-save)
- `frontend/src/components/notes/NotesActionBar.vue` — panneau gauche 48px
- `frontend/src/components/notes/NotesTabs.vue` — barre d'onglets
- `frontend/src/components/notes/NotesEditor.vue` — éditeur toggle Éditer/Aperçu
- `frontend/src/components/notes/FilesModal.vue` — modal explorateur liste/grille
- `frontend/src/components/notes/BobPanel.vue` — panneau Bob (placeholder)
- `frontend/src/__tests__/notes.store.test.ts` — tests Vitest du store

**Modifier :**
- `frontend/src/views/NotesView.vue` — remplacer le stub par le layout complet
- `frontend/docs/dev/02-frontend.md` — mettre à jour la structure de fichiers

---

## Task 1 : Installer les dépendances et définir les types TypeScript

**Files:**
- Create: `frontend/src/types/notes.ts`
- Modify: `frontend/package.json` (via npm install)

- [ ] **Step 1 : Installer marked et dompurify**

```bash
cd frontend
npm install marked dompurify
npm install --save-dev @types/dompurify
```

Expected: no errors. `node_modules/marked` et `node_modules/dompurify` présents.

- [ ] **Step 2 : Créer `src/types/notes.ts`**

```typescript
export interface Note {
  id: number;
  title: string;
  content: string;
  folder_id: number | null;
  is_favorite: boolean;
  created_at: string;
  updated_at: string;
}

export interface Folder {
  id: number;
  name: string;
  parent_id: number | null;
  created_at: string;
}
```

- [ ] **Step 3 : Vérifier que TypeScript accepte les types**

```bash
cd frontend
npx vue-tsc --noEmit
```

Expected: aucune erreur de type.

---

## Task 2 : Notes store (TDD)

**Files:**
- Create: `frontend/src/__tests__/notes.store.test.ts`
- Create: `frontend/src/stores/notes.ts`

- [ ] **Step 1 : Écrire les tests dans `src/__tests__/notes.store.test.ts`**

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { setActivePinia, createPinia } from "pinia";
import { useNotesStore } from "../stores/notes";
import type { Note } from "../types/notes";

function mockNote(id: number): Note {
  return {
    id,
    title: `Note ${id}`,
    content: `Content ${id}`,
    folder_id: null,
    is_favorite: false,
    created_at: "2026-05-03T00:00:00Z",
    updated_at: "2026-05-03T00:00:00Z",
  };
}

describe("notes store", () => {
  beforeEach(() => {
    setActivePinia(createPinia());
    vi.useFakeTimers();
  });

  afterEach(() => {
    vi.useRealTimers();
    vi.unstubAllGlobals();
  });

  it("has correct default state", () => {
    const store = useNotesStore();
    expect(store.openTabs).toEqual([]);
    expect(store.activeTabId).toBeNull();
    expect(store.isBobVisible).toBe(false);
    expect(store.pendingContent).toBe("");
    expect(store.pendingTitle).toBe("");
    expect(store.saveStatus).toBe("idle");
  });

  it("openNote — fetches and adds new tab", async () => {
    vi.stubGlobal(
      "fetch",
      vi.fn().mockResolvedValue({ ok: true, json: () => Promise.resolve(mockNote(1)) })
    );
    const store = useNotesStore();
    await store.openNote(1);
    expect(store.openTabs).toHaveLength(1);
    expect(store.activeTabId).toBe(1);
    expect(store.pendingContent).toBe("Content 1");
    expect(store.pendingTitle).toBe("Note 1");
  });

  it("openNote — already open: only sets active tab without fetching", async () => {
    const fetchMock = vi
      .fn()
      .mockResolvedValue({ ok: true, json: () => Promise.resolve(mockNote(1)) });
    vi.stubGlobal("fetch", fetchMock);
    const store = useNotesStore();
    await store.openNote(1);
    await store.openNote(1);
    expect(fetchMock).toHaveBeenCalledTimes(1);
    expect(store.openTabs).toHaveLength(1);
  });

  it("closeTab — active tab activates the previous tab", async () => {
    let call = 0;
    vi.stubGlobal(
      "fetch",
      vi.fn().mockResolvedValue({ ok: true, json: () => Promise.resolve(mockNote(++call)) })
    );
    const store = useNotesStore();
    await store.openNote(1);
    await store.openNote(2);
    await store.closeTab(2);
    expect(store.activeTabId).toBe(1);
    expect(store.openTabs).toHaveLength(1);
  });

  it("closeTab — last tab clears activeTabId", async () => {
    vi.stubGlobal(
      "fetch",
      vi.fn().mockResolvedValue({ ok: true, json: () => Promise.resolve(mockNote(1)) })
    );
    const store = useNotesStore();
    await store.openNote(1);
    await store.closeTab(1);
    expect(store.activeTabId).toBeNull();
    expect(store.openTabs).toHaveLength(0);
  });

  it("toggleBob — flips isBobVisible", () => {
    const store = useNotesStore();
    expect(store.isBobVisible).toBe(false);
    store.toggleBob();
    expect(store.isBobVisible).toBe(true);
    store.toggleBob();
    expect(store.isBobVisible).toBe(false);
  });

  it("toggleFavorite — sends PATCH with inverted is_favorite", async () => {
    const fetchMock = vi
      .fn()
      .mockResolvedValueOnce({ ok: true, json: () => Promise.resolve(mockNote(1)) })
      .mockResolvedValue({});
    vi.stubGlobal("fetch", fetchMock);
    const store = useNotesStore();
    await store.openNote(1);
    await store.toggleFavorite(1);
    expect(fetchMock).toHaveBeenLastCalledWith(
      "http://localhost:8000/api/notes/1",
      expect.objectContaining({
        method: "PATCH",
        body: JSON.stringify({ is_favorite: true }),
      })
    );
  });
});
```

- [ ] **Step 2 : Lancer les tests pour confirmer qu'ils échouent**

```bash
cd frontend
npm test
```

Expected: FAIL — `Cannot find module '../stores/notes'`

- [ ] **Step 3 : Créer `src/stores/notes.ts`**

```typescript
import { defineStore } from "pinia";
import type { Note, Folder } from "../types/notes";

const API_BASE = "http://localhost:8000";

export type SaveStatus = "idle" | "saving" | "saved" | "error";

// Timer hors du state pour éviter les problèmes de sérialisation Pinia
let saveTimer: ReturnType<typeof setTimeout> | null = null;

export const useNotesStore = defineStore("notes", {
  state: () => ({
    openTabs: [] as Note[],
    activeTabId: null as number | null,
    isBobVisible: false,
    pendingContent: "",
    pendingTitle: "",
    saveStatus: "idle" as SaveStatus,
    allNotes: [] as Note[],
    folders: [] as Folder[],
  }),

  getters: {
    activeNote(state): Note | undefined {
      return state.openTabs.find((n) => n.id === state.activeTabId);
    },
  },

  actions: {
    async fetchNotes() {
      const res = await fetch(`${API_BASE}/api/notes/`);
      this.allNotes = await res.json();
    },

    async fetchFolders() {
      const res = await fetch(`${API_BASE}/api/folders/`);
      this.folders = await res.json();
    },

    async openNote(id: number) {
      const existing = this.openTabs.find((n) => n.id === id);
      if (existing) {
        this.setActiveTab(id);
        return;
      }
      const res = await fetch(`${API_BASE}/api/notes/${id}`);
      if (!res.ok) throw new Error("Note not found");
      const note: Note = await res.json();
      this.openTabs.push(note);
      this.setActiveTab(id);
    },

    setActiveTab(id: number) {
      this.activeTabId = id;
      const note = this.openTabs.find((n) => n.id === id);
      if (note) {
        this.pendingContent = note.content;
        this.pendingTitle = note.title;
        this.saveStatus = "idle";
      }
    },

    async closeTab(id: number) {
      if (saveTimer) {
        clearTimeout(saveTimer);
        saveTimer = null;
        if (this.activeTabId === id) await this.saveNote();
      }
      const idx = this.openTabs.findIndex((n) => n.id === id);
      this.openTabs.splice(idx, 1);
      if (this.activeTabId === id) {
        const next = this.openTabs[idx - 1] ?? this.openTabs[0];
        if (next) {
          this.setActiveTab(next.id);
        } else {
          this.activeTabId = null;
          this.pendingContent = "";
          this.pendingTitle = "";
          this.saveStatus = "idle";
        }
      }
    },

    scheduleSave() {
      if (saveTimer) clearTimeout(saveTimer);
      this.saveStatus = "saving";
      saveTimer = setTimeout(() => this.saveNote(), 1000);
    },

    async saveNote() {
      if (this.activeTabId === null) return;
      try {
        await fetch(`${API_BASE}/api/notes/${this.activeTabId}`, {
          method: "PATCH",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            title: this.pendingTitle,
            content: this.pendingContent,
          }),
        });
        const tab = this.openTabs.find((n) => n.id === this.activeTabId);
        if (tab) {
          tab.title = this.pendingTitle;
          tab.content = this.pendingContent;
        }
        this.saveStatus = "saved";
      } catch {
        this.saveStatus = "error";
        setTimeout(() => this.saveNote(), 3000);
      }
    },

    async toggleFavorite(id: number) {
      const note =
        this.openTabs.find((n) => n.id === id) ??
        this.allNotes.find((n) => n.id === id);
      if (!note) return;
      await fetch(`${API_BASE}/api/notes/${id}`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ is_favorite: !note.is_favorite }),
      });
      note.is_favorite = !note.is_favorite;
    },

    async createNote(folderId?: number) {
      const res = await fetch(`${API_BASE}/api/notes/`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          title: "Sans titre",
          content: "",
          folder_id: folderId ?? null,
        }),
      });
      const note: Note = await res.json();
      this.allNotes.push(note);
      this.openTabs.push(note);
      this.setActiveTab(note.id);
    },

    async deleteNote(id: number) {
      await fetch(`${API_BASE}/api/notes/${id}`, { method: "DELETE" });
      this.allNotes = this.allNotes.filter((n) => n.id !== id);
      if (this.openTabs.find((n) => n.id === id)) {
        await this.closeTab(id);
      }
    },

    async createFolder(name: string, parentId?: number) {
      await fetch(`${API_BASE}/api/folders/`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ name, parent_id: parentId ?? null }),
      });
      await this.fetchFolders();
    },

    async deleteFolder(id: number) {
      await fetch(`${API_BASE}/api/folders/${id}`, { method: "DELETE" });
      await this.fetchFolders();
    },

    toggleBob() {
      this.isBobVisible = !this.isBobVisible;
    },
  },
});
```

- [ ] **Step 4 : Lancer les tests pour confirmer qu'ils passent**

```bash
cd frontend
npm test
```

Expected: les 7 nouveaux tests passent. Les 16 tests existants passent toujours (total : 23 tests).

---

## Task 3 : BobPanel (placeholder)

**Files:**
- Create: `frontend/src/components/notes/BobPanel.vue`

- [ ] **Step 1 : Créer le répertoire components/notes**

```bash
mkdir -p frontend/src/components/notes
```

- [ ] **Step 2 : Créer `src/components/notes/BobPanel.vue`**

```vue
<template>
  <aside class="bob-panel">
    <div class="bob-header">
      <span class="bob-title">Bob</span>
    </div>
    <div class="bob-content">
      <p class="bob-placeholder">À venir — Plan 6</p>
    </div>
  </aside>
</template>

<style scoped>
.bob-panel {
  width: 300px;
  background-color: var(--color-bg-secondary);
  border-left: 1px solid var(--color-bg-tertiary);
  display: flex;
  flex-direction: column;
  flex-shrink: 0;
}

.bob-header {
  padding: 12px 16px;
  border-bottom: 1px solid var(--color-bg-tertiary);
}

.bob-title {
  font-size: 14px;
  font-weight: 600;
  color: var(--color-accent-primary);
}

.bob-content {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
}

.bob-placeholder {
  font-size: 13px;
  color: var(--color-text-muted);
}
</style>
```

---

## Task 4 : NotesActionBar

**Files:**
- Create: `frontend/src/components/notes/NotesActionBar.vue`

- [ ] **Step 1 : Créer `src/components/notes/NotesActionBar.vue`**

```vue
<script setup lang="ts">
const emit = defineEmits<{
  (e: "open-modal"): void;
  (e: "open-favorites"): void;
  (e: "new-note"): void;
}>();
</script>

<template>
  <aside class="action-bar">
    <button class="action-btn" title="Fichiers" @click="emit('open-modal')">📁</button>
    <button class="action-btn" title="Favoris" @click="emit('open-favorites')">⭐</button>
    <button class="action-btn" title="Nouvelle note" @click="emit('new-note')">✏️</button>
  </aside>
</template>

<style scoped>
.action-bar {
  width: 48px;
  background-color: var(--color-bg-secondary);
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 12px 0;
  gap: 8px;
  flex-shrink: 0;
  border-right: 1px solid var(--color-bg-tertiary);
}

.action-btn {
  width: 36px;
  height: 36px;
  background: transparent;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 18px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background-color 0.15s ease;
}

.action-btn:hover {
  background-color: var(--color-bg-tertiary);
}
</style>
```

---

## Task 5 : NotesTabs

**Files:**
- Create: `frontend/src/components/notes/NotesTabs.vue`

- [ ] **Step 1 : Créer `src/components/notes/NotesTabs.vue`**

```vue
<script setup lang="ts">
import { useNotesStore } from "../../stores/notes";

const store = useNotesStore();
</script>

<template>
  <div class="tabs-bar">
    <button
      v-for="note in store.openTabs"
      :key="note.id"
      class="tab"
      :class="{ active: note.id === store.activeTabId }"
      @click="store.setActiveTab(note.id)"
    >
      <span class="tab-title">
        <span
          v-if="note.id === store.activeTabId && store.saveStatus === 'saving'"
          class="dot"
        >•</span>
        {{ note.title || "Sans titre" }}
      </span>
      <span
        v-if="note.id === store.activeTabId && store.saveStatus === 'error'"
        class="error-indicator"
      >⚠</span>
      <button class="tab-close" @click.stop="store.closeTab(note.id)">✕</button>
    </button>
  </div>
</template>

<style scoped>
.tabs-bar {
  display: flex;
  background-color: var(--color-bg-secondary);
  border-bottom: 1px solid var(--color-bg-tertiary);
  overflow-x: auto;
  flex-shrink: 0;
  min-height: 36px;
}

.tab {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 8px 12px;
  background: transparent;
  border: none;
  border-right: 1px solid var(--color-bg-tertiary);
  color: var(--color-text-secondary);
  font-size: 13px;
  cursor: pointer;
  white-space: nowrap;
  transition: background-color 0.15s ease, color 0.15s ease;
  max-width: 180px;
}

.tab:hover {
  background-color: var(--color-bg-tertiary);
  color: var(--color-text-primary);
}

.tab.active {
  color: var(--color-accent-primary);
  background-color: var(--color-bg-primary);
  border-bottom: 2px solid var(--color-accent-primary);
}

.tab-title {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  flex: 1;
  min-width: 0;
}

.dot {
  color: var(--color-warning);
}

.error-indicator {
  color: var(--color-error);
  font-size: 11px;
}

.tab-close {
  background: transparent;
  border: none;
  color: var(--color-text-muted);
  cursor: pointer;
  font-size: 11px;
  padding: 2px 4px;
  border-radius: 3px;
  flex-shrink: 0;
  line-height: 1;
}

.tab-close:hover {
  color: var(--color-text-primary);
  background-color: var(--color-bg-tertiary);
}
</style>
```

---

## Task 6 : NotesEditor

**Files:**
- Create: `frontend/src/components/notes/NotesEditor.vue`

- [ ] **Step 1 : Créer `src/components/notes/NotesEditor.vue`**

```vue
<script setup lang="ts">
import { ref, computed, watch } from "vue";
import { marked } from "marked";
import DOMPurify from "dompurify";
import { useNotesStore } from "../../stores/notes";

const store = useNotesStore();
const mode = ref<"edit" | "preview">("edit");

const renderedContent = computed(() =>
  DOMPurify.sanitize(marked.parse(store.pendingContent) as string)
);

function onTitleInput(e: Event) {
  store.pendingTitle = (e.target as HTMLInputElement).value;
  store.scheduleSave();
}

function onContentInput(e: Event) {
  store.pendingContent = (e.target as HTMLTextAreaElement).value;
  store.scheduleSave();
}

watch(
  () => store.activeTabId,
  () => {
    mode.value = "edit";
  }
);
</script>

<template>
  <div class="editor">
    <div class="editor-toolbar">
      <input
        class="title-input"
        :value="store.pendingTitle"
        placeholder="Sans titre"
        @input="onTitleInput"
      />
      <div class="toolbar-right">
        <button
          class="mode-btn"
          :class="{ active: mode === 'edit' }"
          @click="mode = 'edit'"
        >✏️ Éditer</button>
        <button
          class="mode-btn"
          :class="{ active: mode === 'preview' }"
          @click="mode = 'preview'"
        >👁 Aperçu</button>
        <button class="icon-btn" @click="store.toggleFavorite(store.activeTabId!)">
          {{ store.activeNote?.is_favorite ? "⭐" : "☆" }}
        </button>
        <button class="icon-btn" :class="{ active: store.isBobVisible }" @click="store.toggleBob()">
          Bob
        </button>
      </div>
    </div>

    <textarea
      v-if="mode === 'edit'"
      class="editor-textarea"
      :value="store.pendingContent"
      placeholder="Écris ta note en Markdown..."
      @input="onContentInput"
    />
    <div
      v-else
      class="editor-preview"
      v-html="renderedContent"
    />
  </div>
</template>

<style scoped>
.editor {
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.editor-toolbar {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  background-color: var(--color-bg-primary);
  border-bottom: 1px solid var(--color-bg-tertiary);
  flex-shrink: 0;
}

.title-input {
  flex: 1;
  background: transparent;
  border: none;
  color: var(--color-text-primary);
  font-size: 16px;
  font-weight: 500;
  outline: none;
  min-width: 0;
}

.title-input::placeholder {
  color: var(--color-text-muted);
}

.toolbar-right {
  display: flex;
  align-items: center;
  gap: 4px;
  flex-shrink: 0;
}

.mode-btn,
.icon-btn {
  background: transparent;
  border: 1px solid var(--color-bg-tertiary);
  color: var(--color-text-secondary);
  padding: 4px 10px;
  border-radius: 4px;
  font-size: 12px;
  cursor: pointer;
  transition: color 0.15s, background-color 0.15s, border-color 0.15s;
}

.mode-btn:hover,
.icon-btn:hover {
  color: var(--color-text-primary);
  background-color: var(--color-bg-tertiary);
}

.mode-btn.active,
.icon-btn.active {
  color: var(--color-accent-primary);
  border-color: var(--color-accent-primary);
}

.editor-textarea {
  flex: 1;
  background-color: var(--color-bg-secondary);
  color: var(--color-text-primary);
  border: none;
  padding: 24px;
  font-family: "Fira Code", "Cascadia Code", monospace;
  font-size: 14px;
  line-height: 1.7;
  resize: none;
  outline: none;
}

.editor-preview {
  flex: 1;
  padding: 24px;
  overflow-y: auto;
  color: var(--color-text-primary);
  line-height: 1.7;
}

.editor-preview :deep(h1),
.editor-preview :deep(h2),
.editor-preview :deep(h3) {
  color: var(--color-text-primary);
  margin: 1em 0 0.5em;
}

.editor-preview :deep(code) {
  background-color: var(--color-bg-secondary);
  padding: 2px 6px;
  border-radius: 3px;
  font-family: "Fira Code", monospace;
  font-size: 13px;
}

.editor-preview :deep(pre) {
  background-color: var(--color-bg-secondary);
  padding: 16px;
  border-radius: 6px;
  overflow-x: auto;
  margin: 1em 0;
}

.editor-preview :deep(pre code) {
  background: none;
  padding: 0;
}

.editor-preview :deep(a) {
  color: var(--color-accent-primary);
}

.editor-preview :deep(blockquote) {
  border-left: 3px solid var(--color-accent-secondary);
  padding-left: 16px;
  color: var(--color-text-secondary);
  margin: 1em 0;
}

.editor-preview :deep(ul),
.editor-preview :deep(ol) {
  padding-left: 24px;
}

.editor-preview :deep(table) {
  border-collapse: collapse;
  width: 100%;
  margin: 1em 0;
}

.editor-preview :deep(th),
.editor-preview :deep(td) {
  border: 1px solid var(--color-bg-tertiary);
  padding: 8px 12px;
  text-align: left;
}

.editor-preview :deep(th) {
  background-color: var(--color-bg-secondary);
}
</style>
```

---

## Task 7 : FilesModal

**Files:**
- Create: `frontend/src/components/notes/FilesModal.vue`

- [ ] **Step 1 : Créer `src/components/notes/FilesModal.vue`**

```vue
<script setup lang="ts">
import { ref, computed } from "vue";
import { useNotesStore } from "../../stores/notes";
import type { Note } from "../../types/notes";

const props = defineProps<{ favoritesOnly: boolean }>();
const emit = defineEmits<{ (e: "close"): void }>();

const store = useNotesStore();
const search = ref("");
const viewMode = ref<"list" | "grid">(
  (localStorage.getItem("notes-view-mode") as "list" | "grid") ?? "list"
);
const newFolderName = ref("");
const showNewFolderFor = ref<number | null>(null);

function setViewMode(mode: "list" | "grid") {
  viewMode.value = mode;
  localStorage.setItem("notes-view-mode", mode);
}

function matches(note: Note) {
  if (!search.value) return true;
  return note.title.toLowerCase().includes(search.value.toLowerCase());
}

const favoriteNotes = computed(() =>
  store.allNotes.filter((n) => n.is_favorite && matches(n))
);

const rootFolders = computed(() =>
  store.folders.filter((f) => f.parent_id === null)
);

function subFolders(parentId: number) {
  return store.folders.filter((f) => f.parent_id === parentId);
}

function notesInFolder(folderId: number) {
  return store.allNotes.filter((n) => n.folder_id === folderId && matches(n));
}

const unfolderedNotes = computed(() =>
  store.allNotes.filter((n) => n.folder_id === null && matches(n))
);

function formatDate(dateStr: string) {
  return new Date(dateStr).toLocaleDateString("fr-FR", {
    day: "numeric",
    month: "short",
  });
}

async function selectNote(id: number) {
  await store.openNote(id);
  emit("close");
}

async function handleCreateNote(folderId?: number) {
  await store.createNote(folderId);
  emit("close");
}

async function submitNewFolder(parentId: number) {
  if (!newFolderName.value.trim()) return;
  await store.createFolder(newFolderName.value.trim(), parentId);
  newFolderName.value = "";
  showNewFolderFor.value = null;
}
</script>

<template>
  <div class="modal-overlay" @click.self="emit('close')">
    <div class="modal">
      <div class="modal-header">
        <span class="modal-title">Mes fichiers</span>
        <div class="modal-controls">
          <input v-model="search" class="search-input" placeholder="Rechercher..." />
          <button class="view-btn" :class="{ active: viewMode === 'list' }" @click="setViewMode('list')">≡</button>
          <button class="view-btn" :class="{ active: viewMode === 'grid' }" @click="setViewMode('grid')">⊞</button>
          <button class="close-btn" @click="emit('close')">✕</button>
        </div>
      </div>

      <div class="modal-body">
        <!-- Favoris -->
        <div v-if="favoriteNotes.length > 0" class="section">
          <div class="section-label">⭐ Favoris</div>
          <div :class="['notes-grid', viewMode]">
            <div
              v-for="note in favoriteNotes"
              :key="note.id"
              class="note-item"
              @click="selectNote(note.id)"
            >
              <span class="note-icon">📄</span>
              <span class="note-name">{{ note.title || "Sans titre" }}</span>
              <span v-if="viewMode === 'list'" class="note-date">{{ formatDate(note.updated_at) }}</span>
            </div>
          </div>
        </div>

        <!-- Dossiers (masqués si favoritesOnly) -->
        <template v-if="!props.favoritesOnly">
          <div v-for="folder in rootFolders" :key="folder.id" class="section">
            <div class="folder-row">
              <span class="folder-label">📁 {{ folder.name }}</span>
              <div class="folder-actions">
                <button class="mini-btn" @click="handleCreateNote(folder.id)">+ note</button>
                <button class="mini-btn" @click="showNewFolderFor = folder.id">+ 📁</button>
              </div>
            </div>
            <div v-if="showNewFolderFor === folder.id" class="new-folder-row">
              <input
                v-model="newFolderName"
                class="new-folder-input"
                placeholder="Nom du sous-dossier"
                @keyup.enter="submitNewFolder(folder.id)"
              />
              <button class="mini-btn" @click="submitNewFolder(folder.id)">OK</button>
            </div>
            <div :class="['notes-grid', viewMode]">
              <div
                v-for="note in notesInFolder(folder.id)"
                :key="note.id"
                class="note-item"
                @click="selectNote(note.id)"
              >
                <span class="note-icon">📄</span>
                <span class="note-name">{{ note.title || "Sans titre" }}</span>
                <span v-if="viewMode === 'list'" class="note-date">{{ formatDate(note.updated_at) }}</span>
              </div>
            </div>
            <!-- Sous-dossiers (1 niveau) -->
            <div v-for="sub in subFolders(folder.id)" :key="sub.id" class="subfolder">
              <div class="folder-row subfolder-indent">
                <span class="folder-label">📁 {{ sub.name }}</span>
                <button class="mini-btn" @click="handleCreateNote(sub.id)">+ note</button>
              </div>
              <div :class="['notes-grid subfolder-indent', viewMode]">
                <div
                  v-for="note in notesInFolder(sub.id)"
                  :key="note.id"
                  class="note-item"
                  @click="selectNote(note.id)"
                >
                  <span class="note-icon">📄</span>
                  <span class="note-name">{{ note.title || "Sans titre" }}</span>
                  <span v-if="viewMode === 'list'" class="note-date">{{ formatDate(note.updated_at) }}</span>
                </div>
              </div>
            </div>
          </div>

          <!-- Notes sans dossier -->
          <div v-if="unfolderedNotes.length > 0" class="section">
            <div class="section-label">Sans dossier</div>
            <div :class="['notes-grid', viewMode]">
              <div
                v-for="note in unfolderedNotes"
                :key="note.id"
                class="note-item"
                @click="selectNote(note.id)"
              >
                <span class="note-icon">📄</span>
                <span class="note-name">{{ note.title || "Sans titre" }}</span>
                <span v-if="viewMode === 'list'" class="note-date">{{ formatDate(note.updated_at) }}</span>
              </div>
            </div>
          </div>
        </template>
      </div>

      <div class="modal-footer">
        <button class="new-note-btn" @click="handleCreateNote()">+ Nouvelle note</button>
      </div>
    </div>
  </div>
</template>

<style scoped>
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
}

.modal {
  background-color: var(--color-bg-secondary);
  border: 1px solid var(--color-bg-tertiary);
  border-radius: 8px;
  width: 560px;
  max-height: 70vh;
  display: flex;
  flex-direction: column;
}

.modal-header {
  display: flex;
  align-items: center;
  padding: 16px;
  border-bottom: 1px solid var(--color-bg-tertiary);
  gap: 12px;
}

.modal-title {
  font-size: 15px;
  font-weight: 600;
  color: var(--color-text-primary);
  flex: 1;
}

.modal-controls {
  display: flex;
  align-items: center;
  gap: 6px;
}

.search-input {
  background-color: var(--color-bg-primary);
  border: 1px solid var(--color-bg-tertiary);
  color: var(--color-text-primary);
  padding: 4px 10px;
  border-radius: 4px;
  font-size: 13px;
  width: 150px;
  outline: none;
}

.view-btn {
  background: transparent;
  border: 1px solid var(--color-bg-tertiary);
  color: var(--color-text-secondary);
  padding: 4px 8px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 14px;
  transition: color 0.15s, border-color 0.15s;
}

.view-btn.active {
  color: var(--color-accent-primary);
  border-color: var(--color-accent-primary);
}

.close-btn {
  background: transparent;
  border: none;
  color: var(--color-text-muted);
  cursor: pointer;
  font-size: 16px;
  padding: 4px;
}

.modal-body {
  flex: 1;
  overflow-y: auto;
  padding: 8px 16px;
}

.section {
  margin-bottom: 8px;
}

.section-label {
  font-size: 11px;
  color: var(--color-text-muted);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  padding: 8px 0 4px;
}

.folder-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 6px 0;
}

.subfolder-indent {
  padding-left: 16px;
}

.folder-label {
  font-size: 13px;
  color: var(--color-text-secondary);
  font-weight: 500;
}

.folder-actions {
  display: flex;
  gap: 4px;
}

.mini-btn {
  background: transparent;
  border: 1px solid var(--color-bg-tertiary);
  color: var(--color-text-muted);
  padding: 2px 6px;
  border-radius: 3px;
  font-size: 11px;
  cursor: pointer;
  transition: color 0.15s;
}

.mini-btn:hover {
  color: var(--color-text-primary);
}

.new-folder-row {
  display: flex;
  gap: 6px;
  padding: 4px 0 4px 16px;
}

.new-folder-input {
  flex: 1;
  background-color: var(--color-bg-primary);
  border: 1px solid var(--color-bg-tertiary);
  color: var(--color-text-primary);
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 13px;
  outline: none;
}

/* Vue liste */
.notes-grid.list {
  display: flex;
  flex-direction: column;
}

.notes-grid.list .note-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 5px 8px;
  border-radius: 4px;
  cursor: pointer;
  transition: background-color 0.1s;
}

/* Vue grille */
.notes-grid.grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
  gap: 8px;
  padding: 6px 0;
}

.notes-grid.grid .note-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  padding: 12px 8px;
  border-radius: 6px;
  cursor: pointer;
  border: 1px solid var(--color-bg-tertiary);
  text-align: center;
  transition: background-color 0.1s;
}

.note-item:hover {
  background-color: var(--color-bg-tertiary);
}

.note-icon {
  font-size: 18px;
  flex-shrink: 0;
}

.note-name {
  font-size: 13px;
  color: var(--color-text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  min-width: 0;
  flex: 1;
}

.notes-grid.grid .note-name {
  white-space: normal;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  word-break: break-word;
  font-size: 12px;
}

.note-date {
  font-size: 11px;
  color: var(--color-text-muted);
  margin-left: auto;
  flex-shrink: 0;
}

.modal-footer {
  padding: 12px 16px;
  border-top: 1px solid var(--color-bg-tertiary);
}

.new-note-btn {
  background: transparent;
  border: 1px dashed var(--color-bg-tertiary);
  color: var(--color-text-secondary);
  padding: 8px 16px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 13px;
  width: 100%;
  transition: color 0.15s, border-color 0.15s;
}

.new-note-btn:hover {
  color: var(--color-accent-primary);
  border-color: var(--color-accent-primary);
}
</style>
```

---

## Task 8 : NotesView — layout shell

**Files:**
- Modify: `frontend/src/views/NotesView.vue`

- [ ] **Step 1 : Remplacer le stub par le layout complet**

Remplacer **tout le contenu** de `src/views/NotesView.vue` par :

```vue
<script setup lang="ts">
import { ref, onMounted } from "vue";
import { useNotesStore } from "../stores/notes";
import NotesActionBar from "../components/notes/NotesActionBar.vue";
import NotesTabs from "../components/notes/NotesTabs.vue";
import NotesEditor from "../components/notes/NotesEditor.vue";
import BobPanel from "../components/notes/BobPanel.vue";
import FilesModal from "../components/notes/FilesModal.vue";

const store = useNotesStore();
const isModalOpen = ref(false);
const modalFavoritesOnly = ref(false);

onMounted(async () => {
  await Promise.all([store.fetchNotes(), store.fetchFolders()]).catch(() => {
    // backend indisponible — interface reste vide
  });
});

function openModal(favoritesOnly = false) {
  modalFavoritesOnly.value = favoritesOnly;
  isModalOpen.value = true;
}
</script>

<template>
  <div class="notes-layout">
    <NotesActionBar
      @open-modal="openModal(false)"
      @open-favorites="openModal(true)"
      @new-note="store.createNote()"
    />
    <div class="editor-area">
      <NotesTabs />
      <NotesEditor v-if="store.activeTabId !== null" />
      <div v-else class="empty-state">
        <p>Ouvre une note depuis 📁</p>
      </div>
    </div>
    <BobPanel v-if="store.isBobVisible" />
  </div>
  <FilesModal
    v-if="isModalOpen"
    :favorites-only="modalFavoritesOnly"
    @close="isModalOpen = false"
  />
</template>

<style scoped>
.notes-layout {
  display: flex;
  height: 100%;
  overflow: hidden;
}

.editor-area {
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  min-width: 0;
}

.empty-state {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-text-muted);
  font-size: 14px;
}
</style>
```

- [ ] **Step 2 : Lancer le serveur de dev et tester le flux complet**

```bash
cd frontend
npm run dev
```

Ouvrir `http://localhost:1420` dans le navigateur.

Séquence à tester (backend Docker doit tourner — `docker compose up -d` depuis la racine) :

1. Naviguer vers `/notes` via la sidebar
2. Cliquer 📁 → la modal s'ouvre
3. Cliquer ⊞ → vue grille, cliquer ≡ → vue liste
4. Créer une note via `+ Nouvelle note`
5. La note s'ouvre dans un onglet avec le titre "Sans titre"
6. Modifier le titre → attendre 1s → indicateur "saving" (•) puis disparaît
7. Écrire du Markdown → cliquer "👁 Aperçu" → rendu HTML
8. Cliquer ✕ sur l'onglet → l'onglet se ferme
9. Cliquer "Bob" → panel droit apparaît. Re-cliquer → disparaît.
10. Cliquer ⭐ dans la toolbar → la note est marquée favorite
11. Cliquer ⭐ dans la ActionBar → la modal s'ouvre avec uniquement les favoris

- [ ] **Step 3 : Vérifier que les tests passent toujours**

```bash
cd frontend
npm test
```

Expected: 23 tests passent, 0 échec.

---

## Task 9 : Documentation

**Files:**
- Modify: `frontend/docs/dev/02-frontend.md`

- [ ] **Step 1 : Mettre à jour `docs/dev/02-frontend.md`**

Dans la section "Statut", changer :
```
**Statut :** ✅ Terminé — 16 tests passent
```
par :
```
**Statut :** ✅ Terminé — 23 tests passent
```

Dans la section "Structure des fichiers", ajouter sous `views/` :
```
│   ├── components/
│   │   └── notes/
│   │       ├── NotesActionBar.vue   # Panneau gauche (boutons modal/favoris/créer)
│   │       ├── NotesTabs.vue        # Barre d'onglets
│   │       ├── NotesEditor.vue      # Éditeur Markdown (Éditer / Aperçu WYSIWYG)
│   │       ├── FilesModal.vue       # Explorateur de fichiers (liste/grille)
│   │       └── BobPanel.vue         # Panel Bob (placeholder Plan 6)
│   │
│   ├── types/
│   │   └── notes.ts                 # Interfaces Note et Folder
│   │
│   ├── stores/
│   │   ├── settings.ts              # (existant)
│   │   └── notes.ts                 # useNotesStore — onglets, auto-save, bob
```

---

## Self-review — Couverture de la spec

| Exigence spec | Tâche |
|---------------|-------|
| TypeScript types Note + Folder | Task 1 |
| Store useNotesStore + toutes les actions | Task 2 |
| Tests Vitest 7 cas | Task 2 |
| BobPanel masquable | Task 3 + Task 8 (toggleBob) |
| NotesActionBar 48px — 3 boutons | Task 4 |
| NotesTabs — indicateur saving/error | Task 5 |
| NotesEditor — mode Éditer (textarea monospace) | Task 6 |
| NotesEditor — mode Aperçu (marked + DOMPurify) | Task 6 |
| NotesEditor — titre inline éditable | Task 6 |
| NotesEditor — toggle favori | Task 6 |
| FilesModal — section Favoris | Task 7 |
| FilesModal — arborescence dossiers 1 niveau | Task 7 |
| FilesModal — toggle vue liste/grille (localStorage) | Task 7 |
| FilesModal — recherche temps réel | Task 7 |
| FilesModal — créer note/dossier depuis la modal | Task 7 |
| NotesView — layout shell + empty state | Task 8 |
| Auto-save debounce 1s + retry 3s | Task 2 (scheduleSave + saveNote) |
| Flush immédiat à la fermeture d'onglet | Task 2 (closeTab) |
| Dépendances marked + dompurify | Task 1 |
